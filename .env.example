# Admin API base URL for Vite build.
# Contabo (same host as admin SPA): leave empty — app uses window.location.origin + /api.
# Local dev: http://localhost:4000
# Legacy Render: https://api.nassanitv.com
VITE_API_BASE_URL=
