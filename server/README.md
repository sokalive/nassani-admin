# Nassani Admin — API

Production Node API for Nassani TV (subscriptions, auth, metadata, admin).

Start: `npm start` → `node src/index.js`  
Production: PM2 via `deploy/contabo/ecosystem.config.cjs` (`nassani-admin-api`).
