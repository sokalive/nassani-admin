name: Legacy Render API (disabled)

# Intentionally disabled for Nassani — production is Contabo only.
# Do not reconnect to Osmani Render services from this repository.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: |
          echo "Render deploy is disabled for nassani-admin."
          echo "Use Contabo: deploy/contabo/bootstrap-nassani-vps.sh"
          exit 0
