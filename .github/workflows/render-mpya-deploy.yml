name: Legacy Render Admin (disabled)

# Intentionally disabled for Nassani — production admin is Contabo only.
# Do not reconnect to Osmani Render services from this repository.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: |
          echo "Render admin deploy is disabled for nassani-admin."
          exit 0
